# Cross-repository conformance

`snapshot-analyzer.json` is the harness copy of the exact teacher request contract imported from the recorded pre-split gateway revision. Its recorded whole-file hash no longer matches the edited local gateway working tree. Before either Milk repository receives its initial commit, cross-repository conformance must bind this fixture to the committed `milk-gateway` source and compare the exact prompt and response schema. Later contract changes require coordinated commits in both repositories; the jobs runtime image must bind the admitted gateway contract digest.
